name: Stoat Notification
on: [push, pull_request, issues]

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - name: Send Notification
        run: |
          # Mesajı parça parça oluşturalım ki hata çıkmasın
          REPO="${{ github.repository }}"
          USER="${{ github.actor }}"
          EVENT="${{ github.event_name }}"
          SHA="${{ github.sha }}"
          
          # İçeriği tek bir satırda toplayalım
          MSG="🚀 **Yeni Push!**\n👤 **Yapan:** $USER\n📝 **Repo:** $REPO\n🔗 [Commit Detayları](https://github.com/$REPO/commit/$SHA)"

          # CURL ile gönderelim
          curl -X POST https://stoat.chat/api/webhooks/01KGQ0D3T3FW9EDDFD21EQZVHP/jnEjItkmdDBM_XynlZsJxQbZ-Y9Wd0Hyt3e-8KH-TQoariGhWPgYrw-_GAfY4uZH/ \
          -H "Content-Type: application/json" \
          -d "{\"content\": \"$MSG\"}"
